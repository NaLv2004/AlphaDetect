GNN Grafting Training Log
Started: 2026-04-22 00:32:18
Config: {'gens': 3, 'snr_start': 20.0, 'snr_target': 24.0, 'phase_thresh': 0.5, 'proposals': 20, 'top_k': 20, 'pool_size': 91, 'n_trials': 1, 'timeout': 0.3, 'eval_workers': 2, 'warmstart_gens': 1, 'warmstart_trials': 1, 'warmstart_timeout': 0.15, 'warmstart_eval_workers': 12, 'warmstart_survivor_cap': 32, 'train_steps': 5, 'train_interval': 1, 'buffer_size': 20000, 'pair_temp': 0.7, 'pair_eps': 0.1, 'region_eps': 0.1, 'donor_eps': 0.1, 'ckpt_interval': 1000, 'seed': 42, 'resume': None}


================================================================================
GENERATION 1 | SNR=20dB | 32 grafted in population
================================================================================

--- #1 | algo_5529bf99 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 8}
  Parents: ['turbo_linear', 'dp_detect']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #2 | algo_14620227 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 8}
  Parents: ['turbo_linear', 'variational']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #3 | algo_06a65629 | SER=0.000000 | score=0.002980 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-kbest
  Graft ops: 19 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 8}
  Parents: ['turbo_linear', 'kbest']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        root = _make_tree_node(Nt - 1, [], 0.0)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        candidates = [root]
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #4 | algo_b27f1cf3 | SER=0.000000 | score=0.004940 | verdict=NON_TRIVIAL ---
  Lineage: kbest<-amp
  Graft ops: 4 (dead: 0)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'amp']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        it = 0
        Nt = H.shape[1]
        Nr = H.shape[it]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.conj().T @ y
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                node = candidates[ci]
                node.level = level
                chil
      ... (2005 chars total)

--- #5 | algo_60ef83af | SER=0.000000 | score=0.004940 | verdict=NON_TRIVIAL ---
  Lineage: kbest<-sa
  Graft ops: 4 (dead: 0)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'sa']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        Nr = H.shape[j]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.conj().T @ y
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                node = candidates[ci]
                node.level = level
                childr
      ... (2003 chars total)

--- #6 | algo_40d33415 | SER=0.000000 | score=0.004960 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-alternating
  Graft ops: 10 (dead: 2)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'alternating']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        np.zeros(Nt, dtype=complex)
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.conj().T @ y
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                node = candidates[ci]
                node.level = le
      ... (2025 chars total)

--- #7 | algo_a5ddecc8 | SER=0.000000 | score=0.005140 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-dual_ascent
  Graft ops: 9 (dead: 1)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'dual_ascent']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nr = Nt
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx = order[step]
            cur_idx = co
      ... (1769 chars total)

--- #8 | algo_90871b39 | SER=0.000000 | score=0.005140 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-landweber
  Graft ops: 14 (dead: 2)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'landweber']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        alpha = 0.01
        i = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        x = np.zeros(Nt, dtype=complex)
        root = _make_tree_node(x, [], 0.0)
        y_tilde = Q.conj().T @ y
        candidates = [root]
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                nod
      ... (2071 chars total)

--- #9 | algo_c6b1d3f6 | SER=0.000000 | score=0.005160 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-steepest_desc
  Graft ops: 11 (dead: 1)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'steepest_desc']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nt = H.shape[1]
        detected = H.conj().T
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx = order[step]
            cur_idx = col_map.ind
      ... (1760 chars total)

--- #10 | algo_6a2e6ff7 | SER=0.000000 | score=0.005220 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs_block
  Graft ops: 11 (dead: 1)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs_block']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nr = np.eye(Nt)
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx = order[step]
            cur_
      ... (1777 chars total)

--- #11 | algo_2a655de9 | SER=0.000000 | score=0.005240 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-greedy
  Graft ops: 15 (dead: 2)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'greedy']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        j = 0
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nt = H.shape[1]
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        x = np.zeros(Nt, dtype=complex)
        col_map = list(range(Nt))
        detected = x
        while step < Nt:
            orig_idx = orde
      ... (1797 chars total)

--- #12 | algo_d0cb3687 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #13 | algo_248fda7e | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'kbest']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #14 | algo_51a3d3dc | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 8}
  Parents: ['kbest', 'residual_iter']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #15 | algo_8fa1546a | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'feedback_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #16 | algo_237c70ed | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'soft_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #17 | algo_52c7bafd | SER=0.000000 | score=0.006140 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-projected_gd
  Graft ops: 8 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'projected_gd']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #18 | algo_9d609c4f | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-importance_sampling
  Graft ops: 13 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'importance_sampling']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        best_x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ r
      ... (2647 chars total)

--- #19 | algo_1ea8b533 | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-kaczmarz
  Graft ops: 13 (dead: 3)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'kaczmarz']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = Nt
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        x = np.zeros(Nt, dtype=complex)
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        
      ... (2640 chars total)

--- #20 | algo_d4680a1a | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'stack']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #21 | algo_00af2890 | SER=0.000000 | score=0.006280 | verdict=NON_TRIVIAL ---
  Lineage: osic<-cascade
  Graft ops: 8 (dead: 0)
  Slots: 3 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8, 'stage1': 8}
  Parents: ['osic', 'cascade']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    def _slot_stage1_op_inl_627ba8(H, y, sigma2, constellation):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x_soft = np.linalg.solve(G, H.conj().T @ y)
        x_hat = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
      ... (2201 chars total)

--- #22 | algo_bcc3bb9e | SER=0.000000 | score=0.006280 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-chebyshev
  Graft ops: 14 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'chebyshev']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        i = 0
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        x = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = x * 0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q 
      ... (2660 chars total)

--- #23 | algo_b91cfdfd | SER=0.000000 | score=0.006300 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-variational
  Graft ops: 14 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'variational']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        j = 0
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_
      ... (2652 chars total)

--- #24 | algo_daf250c1 | SER=0.000000 | score=0.006320 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-sparse_detect
  Graft ops: 15 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'sparse_detect']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        lam = sigma2 * 0.1
        np.ones(Nt)
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        x = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = x
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + 
      ... (2685 chars total)

--- #25 | algo_2b0faf67 | SER=0.000000 | score=0.006800 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-two_stage
  Graft ops: 6 (dead: 1)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8, 'hard_decision': 8}
  Parents: ['ep', 'two_stage']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_hard_decision_op_inl_1c5949(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x_refined = y
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        x_hat = _slot_hard_decision_op_inl_1c5949(x_refined, constellation)
        s2inv = 1.0 / max
      ... (2986 chars total)

--- #26 | algo_f5016fa0 | SER=0.000000 | score=0.006880 | verdict=NON_TRIVIAL ---
  Lineage: stack<-nested_iter
  Graft ops: 7 (dead: 0)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'nested_iter']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        _done = 0
        nodes_expanded = 0
        max_nodes = 2000
        outer = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done == 0:
                best_idx = _slot_node_select_op_211(open_
      ... (2280 chars total)

--- #27 | algo_5c13e86c | SER=0.000000 | score=0.006980 | verdict=PARTIAL_DEAD ---
  Lineage: stack<-kaczmarz
  Graft ops: 14 (dead: 3)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'kaczmarz']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        nodes_expanded = 0
        max_nodes = 2000
        found = 0
        _done = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        x = np.zeros(Nt, dtype=complex)
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done == 0:
                best_
      ... (2316 chars total)

--- #28 | algo_e81c6d65 | SER=0.000000 | score=0.007020 | verdict=PARTIAL_DEAD ---
  Lineage: stack<-neumann
  Graft ops: 12 (dead: 1)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'neumann']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        nodes_expanded = 0
        max_nodes = 2000
        i = 0
        _done = 0
        Nr = H.shape[0]
        Nt = H[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        x = np.zeros(Nt, dtype=complex)
        found = x
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done
      ... (2340 chars total)

--- #29 | algo_efb3d961 | SER=0.000000 | score=0.007220 | verdict=NON_TRIVIAL ---
  Lineage: ep<-iter_refine
  Graft ops: 5 (dead: 0)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8, 'stage1': 8}
  Parents: ['ep', 'iter_refine']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_stage1_op_inl_e0dd24(H, y, sigma2, constellation):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x_soft = np.linalg.solve(G, H.conj().T @ y)
        x_hat = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x = _slot_stage1_op_inl_e0dd24(H, y, sigma2, constellation)
        damping = 0.5
        
      ... (3020 chars total)

--- #30 | algo_b17dd132 | SER=0.000000 | score=0.007240 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-soft_sic
  Graft ops: 15 (dead: 3)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'ordering': 8}
  Parents: ['kbest', 'soft_sic']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_ordering_op_inl_a7bfd3(H, y, sigma2):
        Nt = H.shape[1]
        norms = np.zeros(Nt)
        j = 0
        while j < Nt:
            norms[j] = float(np.real(np.sum(np.abs(_col(H, j)) ** 2)))
            j = j + 1
        order = []
        used = np.zeros(Nt)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if used[j] == 0.0:
                    if norms[j] > best_val:
                        be
      ... (2714 chars total)

--- #31 | algo_e4b19b76 | SER=0.000000 | score=0.009740 | verdict=PARTIAL_DEAD ---
  Lineage: amp<-gs
  Graft ops: 14 (dead: 2)
  Slots: 2 | Slot pops: {'amp.amp_iterate': 8, 'amp.final_decision': 8}
  Parents: ['amp', 'gs']
  Source:
    def _slot_amp_iterate_op_101(G, Gtilde, g_scale, gtilde, yMFtilde, sigma2, x_hat, tau_s, z, constellation):
        Nt = len(x_hat)
        M = len(constellation)
        tau_p = g_scale @ tau_s
        theta_s = 0.5
        theta_z = 0.5
        tau_p_safe = np.maximum(tau_p, 1e-30)
        tau_p_plus_s2 = tau_p_safe + sigma2
        tau_z = tau_p_plus_s2 * gtilde
        tau_z = np.maximum(tau_z, 1e-30)
        x_new = np.zeros(Nt, dtype=complex)
        x_var = np.zeros(Nt)
        i = 0
        while i < Nt:
            w = np.zeros(M)
            j = 0
            max_e = -1e30
            while j < M:
                dist_j = float(np.abs(z[i] - constellation[j]) ** 2)
                e = 0.0 - dist_j / max(float(tau_z[i]), 1e-30)
                if e > max_e:
                    max_e = e
                w[j] = e
                j = j + 1
            j = 0
            ws = 0.0
            while j < M:
                w[j] = np.exp(w[j] - max_e)
                ws = ws + w[j]
                j = j + 1
            ws = max(ws, 1e-30)
            j = 0
            while j < M:
                w[j] = w[j] / ws
                j = j + 1
            mu_i = 0.0 + 0.0j
            j = 0
            while j < M:
                mu_i = mu_i + w[j] * constellation[j]
                j = j + 1
            x_new[i] = mu_i
            v_i = 0.0
            j = 0
            while j < M:
                v_i = v_i + w[j] * float(np.abs(constellation[j] - mu_i) ** 2)
                j = j + 1
            x_var[i] = v_i
            i = i + 1
        shat_old = x_hat.copy()
        one_m_ts = 1.0 - theta_s
        tau_s_new = theta_s * x_var + one_m_ts * tau_s
        tau_p_new = g_scale
      ... (2968 chars total)

--- #32 | algo_eb3a6950 | SER=0.062500 | score=0.065720 | verdict=PARTIAL_DEAD ---
  Lineage: admm<-sa
  Graft ops: 14 (dead: 2)
  Slots: 4 | Slot pops: {'admm.x_update': 8, 'admm.z_update': 9, 'admm.dual_update': 9, 'admm.hard_decision': 8}
  Parents: ['admm', 'sa']
  Source:
    def _slot_x_update_op_45(H, y, sigma2, z, u):
        Nt = H.shape[1]
        rho = 1.0
        G = H.conj().T @ H + (sigma2 + rho) * np.eye(Nt)
        rhs = H.conj().T @ y + rho * (z - u)
        return np.linalg.solve(G, rhs)
    
    def _slot_z_update_op_46(x, u, constellation):
        Nt = len(x)
        z = np.zeros(Nt, dtype=complex)
        v = x + u
        i = 0
        while i < Nt:
            dists = np.abs(constellation - v[i]) ** 2
            z[i] = constellation[np.argmin(dists)]
            i = i + 1
        return z
    
    def _slot_dual_update_op_47(u, x, z):
        return u + x - z
    
    def _slot_hard_decision_op_48(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def admm_detector(H, y, sigma2, constellation):
        Nt = H.shape[1]
        Nt = H.shape[1]
        i = Nt
        x = np.zeros(Nt, dtype=complex)
        z = np.zeros(Nt, dtype=complex)
        u = np.zeros(Nt, dtype=complex)
        while i < 30:
            x = _slot_x_update_op_45(H, y, sigma2, z, u)
            z = _slot_z_update_op_46(x, u, constellation)
            u = _slot_dual_update_op_47(u, x, z)
            i = i + 1
        x_hat = _slot_hard_decision_op_48(z, constellation)
        return x_hat

SUMMARY: 8 non-trivial, 0 dead-code, 0 identity, 0 cascade, 32 total


================================================================================
GENERATION 2 | SNR=20dB | 53 grafted in population
================================================================================

--- #1 | algo_5529bf99 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 8}
  Parents: ['turbo_linear', 'dp_detect']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #2 | algo_19c8c026 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_5529bf99', 'algo_daf250c1']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #3 | algo_806dbb32 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_5529bf99', 'lmmse']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #4 | algo_14620227 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['turbo_linear', 'variational']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #5 | algo_7bec1425 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_14620227', 'linear_solve_wrap']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #6 | algo_547cf574 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_14620227', 'algo_8122fd5c']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #7 | algo_1bd2ead8 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_14620227', 'algo_5529bf99']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #8 | algo_06a65629 | SER=0.000000 | score=0.002980 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-kbest
  Graft ops: 19 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['turbo_linear', 'kbest']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        root = _make_tree_node(Nt - 1, [], 0.0)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        candidates = [root]
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #9 | algo_90871b39 | SER=0.000000 | score=0.005140 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-landweber
  Graft ops: 14 (dead: 2)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'landweber']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        alpha = 0.01
        i = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        x = np.zeros(Nt, dtype=complex)
        root = _make_tree_node(x, [], 0.0)
        y_tilde = Q.conj().T @ y
        candidates = [root]
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                nod
      ... (2071 chars total)

--- #10 | algo_6a2e6ff7 | SER=0.000000 | score=0.005220 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs_block
  Graft ops: 11 (dead: 1)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs_block']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nr = np.eye(Nt)
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx = order[step]
            cur_
      ... (1777 chars total)

--- #11 | algo_d0cb3687 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #12 | algo_71ca2e1a | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_d0cb3687', 'sor']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #13 | algo_1d09aba3 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 9, 'osic.sic_step': 8}
  Parents: ['algo_d0cb3687', 'algo_e4b19b76']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #14 | algo_cbede771 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_d0cb3687', 'algo_d4680a1a']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #15 | algo_248fda7e | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'kbest']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #16 | algo_0cec0f70 | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_248fda7e', 'algo_aa1e6470']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #17 | algo_a8ca1d5b | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_248fda7e', 'algo_59be9db8']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #18 | algo_51a3d3dc | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 8}
  Parents: ['kbest', 'residual_iter']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #19 | algo_5b7b5b11 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_645bf301']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #20 | algo_e08007cc | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_ceb82e65']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #21 | algo_8fa1546a | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'feedback_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #22 | algo_237c70ed | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'soft_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #23 | algo_2c850c2d | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_237c70ed', 'algo_d0cb1170']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #24 | algo_549c68fc | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_8fa1546a', 'linear_solve_wrap']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #25 | algo_cea52652 | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 11, 'ep.site_update': 9, 'ep.final_decision': 9}
  Parents: ['algo_8fa1546a', 'algo_1ea8b533']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #26 | algo_3e3f59ed | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_8fa1546a', 'algo_88e346ac']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #27 | algo_169aff91 | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_237c70ed', 'algo_645bf301']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #28 | algo_52c7bafd | SER=0.000000 | score=0.006140 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-projected_gd
  Graft ops: 8 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'projected_gd']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #29 | algo_9d609c4f | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-importance_sampling
  Graft ops: 13 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'importance_sampling']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        best_x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ r
      ... (2647 chars total)

--- #30 | algo_1ea8b533 | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-kaczmarz
  Graft ops: 13 (dead: 3)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'kaczmarz']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = Nt
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        x = np.zeros(Nt, dtype=complex)
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        
      ... (2640 chars total)

--- #31 | algo_066eaa8e | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-importance_sampling
  Graft ops: 13 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_9d609c4f', 'linear_solve_wrap']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        best_x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ r
      ... (2647 chars total)

--- #32 | algo_b77bbfc6 | SER=0.000000 | score=0.006240 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-importance_sampling
  Graft ops: 13 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_9d609c4f', 'amp']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        best_x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ r
      ... (2647 chars total)

--- #33 | algo_d4680a1a | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'stack']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #34 | algo_049f3253 | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_d4680a1a', 'algo_248fda7e']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #35 | algo_2d388a83 | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_d4680a1a', 'algo_1f36a0f8']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #36 | algo_9b5a53d4 | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_d4680a1a', 'algo_90871b39']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #37 | algo_4498c412 | SER=0.000000 | score=0.006260 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-stack
  Graft ops: 12 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 11, 'ep.site_update': 9, 'ep.final_decision': 9}
  Parents: ['algo_d4680a1a', 'algo_bcc3bb9e']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        Nt = H
        rhs = Nt
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        root = _make_tree_node(Nt - 0, [], 0.0)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        open_set = [root]
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        m
      ... (2664 chars total)

--- #38 | algo_00af2890 | SER=0.000000 | score=0.006280 | verdict=NON_TRIVIAL ---
  Lineage: osic<-cascade
  Graft ops: 8 (dead: 0)
  Slots: 3 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8, 'stage1': 8}
  Parents: ['osic', 'cascade']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    def _slot_stage1_op_inl_627ba8(H, y, sigma2, constellation):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x_soft = np.linalg.solve(G, H.conj().T @ y)
        x_hat = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
      ... (2201 chars total)

--- #39 | algo_bcc3bb9e | SER=0.000000 | score=0.006280 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-chebyshev
  Graft ops: 14 (dead: 1)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'chebyshev']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        i = 0
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        x = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = x * 0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q 
      ... (2660 chars total)

--- #40 | algo_b91cfdfd | SER=0.000000 | score=0.006300 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-variational
  Graft ops: 14 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'variational']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        j = 0
        damping = 0.5
        it = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        x = np.zeros(Nt, dtype=complex)
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_
      ... (2652 chars total)

--- #41 | algo_daf250c1 | SER=0.000000 | score=0.006320 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-sparse_detect
  Graft ops: 15 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'sparse_detect']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        lam = sigma2 * 0.1
        np.ones(Nt)
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        x = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = x
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + 
      ... (2685 chars total)

--- #42 | algo_4330675b | SER=0.000000 | score=0.006320 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-sparse_detect
  Graft ops: 15 (dead: 2)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_daf250c1', 'algo_cb3f94ca']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        lam = sigma2 * 0.1
        np.ones(Nt)
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        x = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = x
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + 
      ... (2685 chars total)

--- #43 | algo_2b0faf67 | SER=0.000000 | score=0.006800 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-two_stage
  Graft ops: 6 (dead: 1)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8, 'hard_decision': 8}
  Parents: ['ep', 'two_stage']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_hard_decision_op_inl_1c5949(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x_refined = y
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        x_hat = _slot_hard_decision_op_inl_1c5949(x_refined, constellation)
        s2inv = 1.0 / max
      ... (2986 chars total)

--- #44 | algo_261bb92c | SER=0.000000 | score=0.006800 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-two_stage
  Graft ops: 6 (dead: 1)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8, 'hard_decision': 9}
  Parents: ['algo_2b0faf67', 'algo_d0cb3687']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_hard_decision_op_inl_1c5949(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x_refined = y
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        x_hat = _slot_hard_decision_op_inl_1c5949(x_refined, constellation)
        s2inv = 1.0 / max
      ... (2986 chars total)

--- #45 | algo_f45f014f | SER=0.000000 | score=0.006800 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-two_stage
  Graft ops: 6 (dead: 1)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8, 'hard_decision': 8}
  Parents: ['algo_2b0faf67', 'stack']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_hard_decision_op_inl_1c5949(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x_refined = y
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        x_hat = _slot_hard_decision_op_inl_1c5949(x_refined, constellation)
        s2inv = 1.0 / max
      ... (2986 chars total)

--- #46 | algo_7fe47d76 | SER=0.000000 | score=0.006800 | verdict=PARTIAL_DEAD ---
  Lineage: ep<-two_stage
  Graft ops: 6 (dead: 1)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9, 'hard_decision': 8}
  Parents: ['algo_2b0faf67', 'algo_06a65629']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_hard_decision_op_inl_1c5949(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x_refined = y
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        x_hat = _slot_hard_decision_op_inl_1c5949(x_refined, constellation)
        s2inv = 1.0 / max
      ... (2986 chars total)

--- #47 | algo_f5016fa0 | SER=0.000000 | score=0.006880 | verdict=NON_TRIVIAL ---
  Lineage: stack<-nested_iter
  Graft ops: 7 (dead: 0)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'nested_iter']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        _done = 0
        nodes_expanded = 0
        max_nodes = 2000
        outer = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done == 0:
                best_idx = _slot_node_select_op_211(open_
      ... (2280 chars total)

--- #48 | algo_5c13e86c | SER=0.000000 | score=0.006980 | verdict=PARTIAL_DEAD ---
  Lineage: stack<-kaczmarz
  Graft ops: 14 (dead: 3)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'kaczmarz']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        nodes_expanded = 0
        max_nodes = 2000
        found = 0
        _done = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        x = np.zeros(Nt, dtype=complex)
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done == 0:
                best_
      ... (2316 chars total)

--- #49 | algo_e81c6d65 | SER=0.000000 | score=0.007020 | verdict=PARTIAL_DEAD ---
  Lineage: stack<-neumann
  Graft ops: 12 (dead: 1)
  Slots: 2 | Slot pops: {'stack.node_select': 8, 'stack.expand': 8}
  Parents: ['stack', 'neumann']
  Source:
    def _slot_node_select_op_211(open_set):
        best_idx = 0
        best_cost = open_set[0].cost
        i = 1
        while i < len(open_set):
            if open_set[i].cost < best_cost:
                best_cost = open_set[i].cost
                best_idx = i
            i = i + 1
        return best_idx
    
    def _slot_expand_op_212(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    
    def stack(H, y, sigma2, constellation):
        nodes_expanded = 0
        max_nodes = 2000
        i = 0
        _done = 0
        Nr = H.shape[0]
        Nt = H[0]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        x = np.zeros(Nt, dtype=complex)
        found = x
        root = _make_tree_node(Nt - 1, [], 0.0)
        best_complete = root
        open_set = [root]
        y_tilde = Q.conj().T @ y
        while _done == 0:
            if len(open_set) == 0:
                _done = 1
            if nodes_expanded >= max_nodes:
                _done = 1
            if _done
      ... (2340 chars total)

--- #50 | algo_efb3d961 | SER=0.000000 | score=0.007220 | verdict=NON_TRIVIAL ---
  Lineage: ep<-iter_refine
  Graft ops: 5 (dead: 0)
  Slots: 4 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8, 'stage1': 8}
  Parents: ['ep', 'iter_refine']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    def _slot_stage1_op_inl_e0dd24(H, y, sigma2, constellation):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x_soft = np.linalg.solve(G, H.conj().T @ y)
        x_hat = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def ep(H, y, sigma2, constellation):
        x = _slot_stage1_op_inl_e0dd24(H, y, sigma2, constellation)
        damping = 0.5
        
      ... (3020 chars total)

SUMMARY: 13 non-trivial, 0 dead-code, 0 identity, 0 cascade, 50 total


================================================================================
GENERATION 3 | SNR=20dB | 58 grafted in population
================================================================================

--- #1 | algo_5529bf99 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 8}
  Parents: ['turbo_linear', 'dp_detect']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #2 | algo_19c8c026 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_5529bf99', 'algo_daf250c1']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #3 | algo_806dbb32 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_5529bf99', 'lmmse']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #4 | algo_741cacf8 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 10, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_5529bf99', 'algo_14620227']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #5 | algo_dab0ee9b | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_19c8c026', 'algo_9b5a53d4']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #6 | algo_b4def752 | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_19c8c026', 'algo_1ea8b533']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #7 | algo_ac692bdd | SER=0.000000 | score=0.002720 | verdict=NON_TRIVIAL ---
  Lineage: turbo_linear<-dp_detect
  Graft ops: 6 (dead: 0)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 10, 'turbo_linear.hard_decision': 12}
  Parents: ['algo_806dbb32', 'algo_06a65629']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #8 | algo_14620227 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 10, 'turbo_linear.hard_decision': 10}
  Parents: ['turbo_linear', 'variational']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #9 | algo_7bec1425 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 9}
  Parents: ['algo_14620227', 'linear_solve_wrap']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #10 | algo_547cf574 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 11}
  Parents: ['algo_14620227', 'algo_8122fd5c']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #11 | algo_1bd2ead8 | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_14620227', 'algo_5529bf99']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #12 | algo_427df63c | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 10, 'turbo_linear.hard_decision': 11}
  Parents: ['algo_14620227', 'algo_d0cb3687']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #13 | algo_e170961a | SER=0.000000 | score=0.002800 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-variational
  Graft ops: 11 (dead: 3)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 10, 'turbo_linear.hard_decision': 11}
  Parents: ['algo_7bec1425', 'algo_361f9370']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        Nt = np.zeros
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #14 | algo_06a65629 | SER=0.000000 | score=0.002980 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-kbest
  Graft ops: 19 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 9}
  Parents: ['turbo_linear', 'kbest']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        root = _make_tree_node(Nt - 1, [], 0.0)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        candidates = [root]
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #15 | algo_a155ff11 | SER=0.000000 | score=0.002980 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-kbest
  Graft ops: 19 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_06a65629', 'algo_8122fd5c']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        root = _make_tree_node(Nt - 1, [], 0.0)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        candidates = [root]
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #16 | algo_864ddbfa | SER=0.000000 | score=0.002980 | verdict=PARTIAL_DEAD ---
  Lineage: turbo_linear<-kbest
  Graft ops: 19 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 8, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_06a65629', 'algo_90871b39']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        j = 0
        Nt = H.shape[1]
        var = np.zeros(Nt)
        rhs = H.conj().T @ y
        root = _make_tree_node(Nt - 1, [], 0.0)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        candidates = [root]
        x = np.linalg.solve(G, rhs)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #17 | algo_52c947ed | SER=0.000000 | score=0.003000 | verdict=PARTIAL_DEAD ---
  Lineage: algo_485cf887<-precond_solve_wrap
  Graft ops: 20 (dead: 2)
  Slots: 2 | Slot pops: {'turbo_linear.soft_estimate': 9, 'turbo_linear.hard_decision': 10}
  Parents: ['algo_485cf887', 'precond_solve_wrap']
  Source:
    def _slot_soft_estimate_op_99(x_soft, variance, constellation):
        Nt = len(x_soft)
        x_out = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            v = max(float(variance[i]), 1e-30) if len(variance) > i else 1.0
            w = np.exp(-np.abs(constellation - x_soft[i]) ** 2 / v)
            w_sum = float(np.sum(w))
            if w_sum < 1e-30:
                x_out[i] = x_soft[i]
            else:
                x_out[i] = np.sum(w * constellation) / w_sum
            i = i + 1
        return x_out
    
    def _slot_hard_decision_op_100(x_soft, constellation):
        x_hat = np.zeros(len(x_soft), dtype=complex)
        i = 0
        while i < len(x_soft):
            dists = np.abs(constellation - x_soft[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    
    def turbo_linear_detector(H, y, sigma2, constellation):
        H.conj()
        j = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        var = np.zeros(Nt)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        x = np.linalg.solve(G, G)
        while j < Nt:
            G_inv_jj = 1.0 / max(float(np.real(G[(j, j)])), 1e-30)
            var[j] = G_inv_jj * sigma2
            j = j + 1
        i = 0
        while i < 5:
            x_soft = _slot_soft_estimate_op_99(x, var, constellation)
            r = y - H @ x_soft
            x = x_soft + np.linalg.solve(G, H.conj().T @ r)
            i = i + 1
        x_hat = _slot_hard_decision_op_100(x, constellation)
        return x_hat

--- #18 | algo_90871b39 | SER=0.000000 | score=0.005140 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-landweber
  Graft ops: 14 (dead: 2)
  Slots: 2 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8}
  Parents: ['kbest', 'landweber']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    
    def kbest(H, y, sigma2, constellation):
        alpha = 0.01
        i = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        x = np.zeros(Nt, dtype=complex)
        root = _make_tree_node(x, [], 0.0)
        y_tilde = Q.conj().T @ y
        candidates = [root]
        while level >= 0:
            new_candidates = []
            ci = 0
            while ci < len(candidates):
                nod
      ... (2071 chars total)

--- #19 | algo_6a2e6ff7 | SER=0.000000 | score=0.005220 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs_block
  Graft ops: 11 (dead: 1)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs_block']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Nr = np.eye(Nt)
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx = order[step]
            cur_
      ... (1777 chars total)

--- #20 | algo_d0cb3687 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'gs']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #21 | algo_71ca2e1a | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_d0cb3687', 'sor']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #22 | algo_1d09aba3 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 9, 'osic.sic_step': 8}
  Parents: ['algo_d0cb3687', 'algo_e4b19b76']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #23 | algo_cbede771 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_d0cb3687', 'algo_d4680a1a']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #24 | algo_220cfb2d | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 10}
  Parents: ['algo_cbede771', 'algo_2c512a90']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #25 | algo_bc147e98 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 10}
  Parents: ['algo_cbede771', 'algo_59be9db8']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #26 | algo_a791f53e | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 9, 'osic.sic_step': 9}
  Parents: ['algo_71ca2e1a', 'algo_066eaa8e']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #27 | algo_ee41f8d2 | SER=0.000000 | score=0.005280 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-gs
  Graft ops: 21 (dead: 3)
  Slots: 2 | Slot pops: {'osic.ordering': 9, 'osic.sic_step': 9}
  Parents: ['algo_cbede771', 'algo_654087bd']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        i = 0
        step = 0
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        x = np.zeros(Nt, dtype=complex)
        detected = np.zeros(Nt, dtype=complex)
        rhs = H.conj().T @ y
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig
      ... (1808 chars total)

--- #28 | algo_248fda7e | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 8}
  Parents: ['osic', 'kbest']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #29 | algo_0cec0f70 | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_248fda7e', 'algo_aa1e6470']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #30 | algo_a8ca1d5b | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 8, 'osic.sic_step': 9}
  Parents: ['algo_248fda7e', 'algo_59be9db8']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #31 | algo_a256895a | SER=0.000000 | score=0.005320 | verdict=PARTIAL_DEAD ---
  Lineage: osic<-kbest
  Graft ops: 19 (dead: 4)
  Slots: 2 | Slot pops: {'osic.ordering': 9, 'osic.sic_step': 8}
  Parents: ['algo_248fda7e', 'algo_654087bd']
  Source:
    def _slot_ordering_op_68(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        G_inv = np.linalg.inv(G)
        snr = np.zeros(Nt, dtype=complex)
        i = 0
        while i < Nt:
            snr[i] = 1.0 / max(float(np.real(G_inv[i, i])), 1e-30) - sigma2
            i = i + 1
        order = []
        used = np.zeros(Nt, dtype=complex)
        k = 0
        while k < Nt:
            best = -1
            best_val = -1e30
            j = 0
            while j < Nt:
                if float(np.real(used[j])) == 0.0:
                    if float(np.real(snr[j])) > best_val:
                        best_val = float(np.real(snr[j]))
                        best = j
                j = j + 1
            order.append(best)
            used[best] = 1.0
            k = k + 1
        return order
    
    def _slot_sic_step_op_69(H, y, sigma2, idx, constellation):
        Nr = H.shape[0]
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        w = np.linalg.solve(G, H.conj().T @ y)
        x_est = w[idx]
        dists = np.abs(constellation - x_est) ** 2
        x_hard = constellation[np.argmin(dists)]
        y_new = y - _col(H, idx) * x_hard
        H_new = np.delete(H, idx, axis=1)
        return x_hard, H_new, y_new
    
    
    def osic(H, y, sigma2, constellation):
        step = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        Nt = H.shape[1]
        H_cur = H.copy()
        y_cur = y.copy()
        Q = np.linalg.qr(H)[0]
        detected = np.zeros(Nt, dtype=complex)
        order = _slot_ordering_op_68(H_cur, y_cur, sigma2)
        col_map = list(range(Nt))
        while step < Nt:
            orig_idx
      ... (1804 chars total)

--- #32 | algo_51a3d3dc | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 8}
  Parents: ['kbest', 'residual_iter']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #33 | algo_5b7b5b11 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_645bf301']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #34 | algo_e08007cc | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_ceb82e65']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #35 | algo_d3063244 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_8122fd5c']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #36 | algo_c6a7d7b6 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 10, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_e08007cc', 'algo_d0cb3687']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #37 | algo_c38efb75 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 9}
  Parents: ['algo_51a3d3dc', 'algo_806dbb32']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #38 | algo_36efe4f5 | SER=0.000000 | score=0.005440 | verdict=PARTIAL_DEAD ---
  Lineage: kbest<-residual_iter
  Graft ops: 6 (dead: 1)
  Slots: 3 | Slot pops: {'kbest.expand': 9, 'kbest.prune': 8, 'init': 10}
  Parents: ['algo_5b7b5b11', 'algo_cf2f43d8']
  Source:
    def _slot_expand_op_149(node, y_tilde, R, constellation):
        level = node.level
        Nt = R.shape[1]
        children = []
        interf = 0.0 + 0.0j
        j_rel = 0
        while j_rel < len(node.symbols):
            j = Nt - 1 - j_rel
            interf = interf + R[level, j] * node.symbols[j_rel]
            j_rel = j_rel + 1
        ci = 0
        while ci < len(constellation):
            sym = constellation[ci]
            residual = y_tilde[level] - R[level, level] * sym - interf
            local_cost = float(np.abs(residual) ** 2)
            total = node.cost + local_cost
            child = _make_tree_node(level - 1, node.symbols + [sym], total)
            children.append(child)
            ci = ci + 1
        return children
    
    def _slot_prune_op_150(candidates, K):
        n = len(candidates)
        i = 0
        while i < n:
            j = i + 1
            while j < n:
                if candidates[j].cost < candidates[i].cost:
                    tmp = candidates[i]
                    candidates[i] = candidates[j]
                    candidates[j] = tmp
                j = j + 1
            i = i + 1
        return candidates[:K]
    
    def _slot_init_op_inl_8f8888(H, y, sigma2):
        Nt = H.shape[1]
        G = H.conj().T @ H + sigma2 * np.eye(Nt)
        rhs = H.conj().T @ y
        return np.linalg.solve(G, rhs)
    
    
    def kbest(H, y, sigma2, constellation):
        x = _slot_init_op_inl_8f8888(H, y, sigma2)
        i = 0
        Nt = H.shape[1]
        Q = np.linalg.qr(H)[0]
        R = np.linalg.qr(H)[1]
        level = Nt - 1
        root = _make_tree_node(Nt - 1, [], 0.0)
        candidates = [root]
        y_tilde = Q.
      ... (2200 chars total)

--- #39 | algo_8fa1546a | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'feedback_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #40 | algo_237c70ed | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['ep', 'soft_sic']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #41 | algo_2c850c2d | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_237c70ed', 'algo_d0cb1170']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #42 | algo_549c68fc | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_8fa1546a', 'linear_solve_wrap']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #43 | algo_cea52652 | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 11, 'ep.site_update': 9, 'ep.final_decision': 9}
  Parents: ['algo_8fa1546a', 'algo_1ea8b533']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #44 | algo_3e3f59ed | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_8fa1546a', 'algo_88e346ac']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #45 | algo_169aff91 | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_237c70ed', 'algo_645bf301']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #46 | algo_b9fcf3ae | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 9, 'ep.final_decision': 8}
  Parents: ['algo_237c70ed', 'algo_361f9370']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #47 | algo_759050e3 | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 9, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_237c70ed', 'algo_361f9370']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #48 | algo_28eaecbb | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-feedback_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 12, 'ep.site_update': 10, 'ep.final_decision': 11}
  Parents: ['algo_cea52652', 'algo_b77bbfc6']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        damping = 0.5
        it = 0
        Nr = H.shape[0]
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.re
      ... (2606 chars total)

--- #49 | algo_02a864be | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 10, 'ep.site_update': 8, 'ep.final_decision': 8}
  Parents: ['algo_237c70ed', 'algo_0cec0f70']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

--- #50 | algo_0e8c864e | SER=0.000000 | score=0.006100 | verdict=NON_TRIVIAL ---
  Lineage: ep<-soft_sic
  Graft ops: 5 (dead: 0)
  Slots: 3 | Slot pops: {'ep.cavity': 10, 'ep.site_update': 8, 'ep.final_decision': 9}
  Parents: ['algo_2c850c2d', 'algo_8122fd5c']
  Source:
    def _slot_final_decision_op_199(mu, constellation):
        x_hat = np.zeros(len(mu), dtype=complex)
        i = 0
        while i < len(mu):
            dists = np.abs(constellation - mu[i]) ** 2
            x_hat[i] = constellation[np.argmin(dists)]
            i = i + 1
        return x_hat
    
    def _slot_cavity_op_197(t, h2, gamma_i, alpha_i):
        return t, h2
    
    def _slot_site_update_op_198(t, h2, constellation, gamma_i, alpha_i):
        diffs = constellation - t
        exponents = -np.abs(diffs) ** 2 / max(2.0 * h2, 1e-30)
        max_exp = float(np.max(np.real(exponents)))
        weights = np.exp(np.real(exponents) - max_exp)
        w_sum = max(float(np.sum(weights)), 1e-30)
        weights = weights / w_sum
        mu_p = np.sum(weights * constellation)
        sigma2_p = float(np.real(np.sum(weights * np.abs(constellation) ** 2))) - float(np.abs(mu_p) ** 2)
        sigma2_p = max(sigma2_p, 5e-7)
        new_alpha = 1.0 / sigma2_p - 1.0 / max(h2, 1e-30)
        new_gamma = mu_p / sigma2_p - t / max(h2, 1e-30)
        return new_alpha, new_gamma
    
    
    def ep(H, y, sigma2, constellation):
        k = 0
        damping = 0.5
        it = 0
        Nr = k
        Nt = H.shape[1]
        s2inv = 1.0 / max(sigma2, 1e-30)
        one_m_damp = 1.0 - damping
        gamma_ep = np.zeros(Nt, dtype=complex)
        HtH = H.conj().T @ H
        Hty = H.conj().T @ y
        alpha = np.ones(Nt) * 2.0
        HtH_scaled = HtH * s2inv
        Hty_scaled = Hty * s2inv
        rhs = Hty_scaled + gamma_ep
        Sigma_q = np.linalg.inv(HtH_scaled + np.diag(alpha))
        mu_q = Sigma_q @ rhs
        while it < 20:
            sig = np.r
      ... (2607 chars total)

SUMMARY: 19 non-trivial, 0 dead-code, 0 identity, 0 cascade, 50 total

